# Database
spring.datasource.url=jdbc:postgresql://localhost:5432/your_db
spring.datasource.username=your_username
spring.datasource.password=${DB_PASSWORD}  # Заменится из ENV

# JWT
jwt.secret=${JWT_SECRET}  # Передается через переменные окружения